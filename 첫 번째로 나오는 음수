class Solution {
    public int solution(int[] numList) {
        for(var i = 0; i < numList.length; i++){
            if(numList[i] < 0){
                return i;
            }
        }
        return -1;
    }
}
