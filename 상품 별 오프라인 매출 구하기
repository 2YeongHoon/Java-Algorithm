SELECT A.PRODUCT_CODE, -- 상품코드
       SUM(A.PRICE * B.SALES_AMOUNT) AS SALES -- 매출액(판매가 * 판매량) 합계
  FROM PRODUCT A 
  JOIN OFFLINE_SALE B ON A.PRODUCT_ID = B.PRODUCT_ID
 GROUP BY A.PRODUCT_CODE
 ORDER BY SALES DESC, A.PRODUCT_CODE
