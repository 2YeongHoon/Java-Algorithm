import java.util.*;

class Solution {
    public int[] solution(int[] num_list) {
        final int skipNum = 5;
        
        return Arrays.stream(num_list)
            .sorted()
            .skip(skipNum)
            .toArray();
    }
}
