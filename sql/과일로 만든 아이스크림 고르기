SELECT HALF.FLAVOR FROM FIRST_HALF HALF
INNER JOIN ICECREAM_INFO INFO
    ON HALF.FLAVOR = INFO.FLAVOR
WHERE TOTAL_ORDER >= 3000 AND INFO.INGREDIENT_TYPE like "%fruit_based%"
ORDER BY TOTAL_ORDER DESC;
