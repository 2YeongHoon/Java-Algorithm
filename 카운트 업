import java.util.List;
import java.util.ArrayList;

class Solution {
    public Integer[] solution(int start, int end) {
        List<Integer> answer = new ArrayList<>();
        for(int i = start; i < end + 1; i++){
            answer.add(i);
        }
        
        return answer.toArray(new Integer[end - start - 1]);
    }
}
